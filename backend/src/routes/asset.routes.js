const r=require('express').Router();const C=require('../controllers/asset.controller');const {requirePermission}=require('../middleware/permission.middleware');
r.get('/',requirePermission('ASSET_VIEW'),C.list);r.post('/',requirePermission('ASSET_CREATE'),C.create);r.get('/:id/history',requirePermission('ASSET_VIEW'),C.history);r.post('/:id/assign',requirePermission('ASSET_ASSIGN'),C.assign);r.post('/:id/return',requirePermission('ASSET_ASSIGN'),C.returnAsset);r.post('/:id/transfer',requirePermission('ASSET_TRANSFER'),C.transfer);r.post('/:id/maintenance',requirePermission('ASSET_MAINTENANCE'),C.createMaintenance);r.patch('/:id/maintenance/:maintenanceId/complete',requirePermission('ASSET_MAINTENANCE'),C.completeMaintenance);r.patch('/:id/lifecycle',requirePermission('ASSET_RETIRE'),C.retire);r.get('/:id',requirePermission('ASSET_VIEW'),C.get);r.put('/:id',requirePermission('ASSET_UPDATE'),C.update);module.exports=r;
