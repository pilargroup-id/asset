const r=require('express').Router();const C=require('../controllers/master.controller');const {requirePermission}=require('../middleware/permission.middleware');r.get('/:type',requirePermission('MASTER_VIEW'),C.list);r.get('/:type/:id',requirePermission('MASTER_VIEW'),C.get);r.post('/:type',requirePermission('MASTER_MANAGE'),C.create);r.put('/:type/:id',requirePermission('MASTER_MANAGE'),C.update);module.exports=r;
