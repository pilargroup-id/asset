const r=require('express').Router();const C=require('../controllers/depreciation.controller');const {requirePermission}=require('../middleware/permission.middleware');
r.get('/policies',requirePermission('DEPRECIATION_VIEW'),C.listPolicies);r.post('/policies',requirePermission('DEPRECIATION_MANAGE'),C.createPolicy);r.put('/policies/:id',requirePermission('DEPRECIATION_MANAGE'),C.updatePolicy);r.put('/category-default',requirePermission('DEPRECIATION_MANAGE'),C.setCategoryDefault);r.get('/assets/:assetId/suggestion',requirePermission('DEPRECIATION_VIEW'),C.suggest);r.get('/assets/:assetId',requirePermission('DEPRECIATION_VIEW'),C.getAsset);r.put('/assets/:assetId/config',requirePermission('DEPRECIATION_MANAGE'),C.configure);r.post('/assets/:assetId/revisions',requirePermission('DEPRECIATION_MANAGE'),C.revise);r.post('/assets/:assetId/generate',requirePermission('DEPRECIATION_MANAGE'),C.generate);r.patch('/assets/:assetId/finalize',requirePermission('DEPRECIATION_MANAGE'),C.finalize);module.exports=r;
